#include "{{contractName}}.hpp"

using namespace eosio;

void {{contractName}}::hi(name user){
    print( "{{contractName}}, ", user);
}

{{#each actions}}
void {{actionName}}({{#each params}}{{type}} {{name}}{{#unless @last}}, {{/unless}}{{/each}}) {
{{code}}
}

{{/each}}
