from ..icon_hello import hello
from tbears.libs.scoretest.score_test_case import ScoreTestCase


class Testhello(ScoreTestCase):

    def setUp(self):
        super().setUp()
        self.score = self.get_score_instance(hello, self.test_account1)

    def test_hi(self):
        self.assertEqual(self.score.hi(), "Hi: None")
