{
  "name": "@aloxide/aloxidesdk",
  "description": "sdk",
  "version": "0.1.0",
  "author": "Quoc Le",
  "license": "Apache-2.0",
  "private": false,
  "publishConfig": {
    "access": "public"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:lecle/aloxide.git"
  },
  "main": "./dist/src/index",
  "files": [
    "dist"
  ],
  "types": "./dist/*.d.ts",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rimraf -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "prepublishOnly": "yarn build",
    "lint": "tslint --project . --fix",
    "test": "jest -o"
  },
  "dependencies": {
    "crypto-js": "3.1.9-1",
    "eosjs": "16.0.9",
    "icon-sdk-js": "^0.0.10",
    "is-invalid-path": "1.0.2",
    "jszip": "^3.5.0"
  }
}
