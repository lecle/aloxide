const { DataTypes } = require('sequelize');

{{#each models}}
const {{name}}Schema = {
  // Model attributes are defined here
  {{#each attributes}}
  {{name}}: {
    type: {{type}},
    // allowNull: false,{{#if primaryKey}}
    autoIncrement: true,
    primaryKey: true,{{/if}}
  },
  {{/each}}
};

{{/each}}
module.exports.bcModelBuilder = (sequelize) => ({
  {{#each models}}
  {{name}}: sequelize.define('{{name}}', {{name}}Schema, {/* Other model options go here */}),
  {{/each}}
});